import React from 'react';
import Navigation from './Navigation';

const Header: React.FC = () => {
  return (
    <header>
      <h1>Aidan Choi</h1>
      <Navigation />
    </header>
  );
};

export default Header;
